function controler() {
    // on crée la variable zoneSaisie, que l'on fait correspond à l'élément HTML ayant l'id zoneSaisie
    var zoneSaisie = document.getElementById("zoneSaisie");
    if (zoneSaisie.value) {
        // on crée une alerte qui affiche le contenu de cette zone 
        alert("La Zone de saisie contient : " + zoneSaisie.value);
    }
    else {
        alert("La zone de saisie est vide !");
    }  
}

// en cours partie 5 

function eval(x) { 
    var regex = #[A-Z0-9][+][A-Z0-9]#;
    var x;
    var y;
    if ()
}